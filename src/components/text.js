import styled from 'styled-components/macro';

const Text = styled.p`
  color: var(--color-text);
  max-width: 80ch;
  line-height: 1.8;
  letter-spacing: .5px;
  text-align: justify;
`;

export default Text;
